/**
 * TELOS Design Tokens — re-exported from the generated module.
 *
 * SINGLE SOURCE OF TRUTH: design-system/tokens.json. Edit that file, then run
 * `npm run sync:design` to regenerate tokens.generated.ts (and the CSS vars in
 * globals.css, plus the portable design-system/tokens.css + tokens.flat.json).
 * Do NOT hand-edit values here — they will be overwritten on the next sync.
 *
 * This thin re-export preserves every existing import
 * (`import { FOUNDATION, ACCENT, TYPE, ... } from "@/brand/tokens"`).
 */
export {
  FOUNDATION,
  ACCENT,
  SEMANTIC,
  TYPE,
  SPACE,
  RADIUS,
  BORDER,
  MOTION,
  TAGLINES,
  LIGHT,
  ROLE,
  ATMOSPHERE,
} from "./tokens.generated";
